<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing to strip any more: since the AI coach the app declares INTERNET
     itself, in the main manifest, with the reasoning there.

     Kept as a file rather than deleted, because this is where a permission a
     dependency drags in would be removed again, and the next one will be
     easier to catch with the pattern still standing. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
</manifest>
